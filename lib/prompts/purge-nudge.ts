export const PURGE_NUDGE_EN = `
Consider using the \`purge\` tool for aggressive compression.
\`purge\` has no content restrictions: it replaces any selected content
regardless of protected tools, tags, or user message settings.
Restorable via \`decompress\` if needed.
`

export const PURGE_NUDGE_ZH = `
考虑使用 \`purge\` 工具进行激进压缩。
Purge 不受内容保护限制：会替换所选范围内的所有内容，
无视 protected tools、标签保护和用户消息保护设置。
可通过 \`decompress\` 恢复。
`
