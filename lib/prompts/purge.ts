export const PURGE = `Aggressively delete or compress ANY conversation content.

This tool has NO restrictions. ALL content can be removed or compressed.

For every message and tool result in the selected range:
- COMPLETELY USELESS -> summary: [purged] (deleted entirely)
- MINIMAL VALUE -> summary: one-line description
- STILL NEEDED -> do NOT include in this range

Do NOT write exhaustive summaries. Be ruthless.

This tool is identical to \`compress\` in format but has no protected content or safety limits.`
